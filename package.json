{
  "name": "@small-tech/https",
  "version": "1.2.0",
  "description": "Drop in replacement for Node https module that automatically handles certificate provisioning both at localhost (via Auto Encrypt Localhost/mkcert) and at hostname (via Auto Encrypt/Let’s Encrypt).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "example": "node example/"
  },
  "author": "Aral Balkan <aral@small-tech.org> (https://ar.al)",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@small-tech/auto-encrypt": "file:../auto-encrypt",
    "@small-tech/auto-encrypt-localhost": "file:../auto-encrypt-localhost"
  }
}
